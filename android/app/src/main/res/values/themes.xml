<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Compose paints everything; this only keeps the window from flashing a stock background
         before the first frame. -->
    <style name="Theme.Six" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/background_dark</item>
    </style>
</resources>
